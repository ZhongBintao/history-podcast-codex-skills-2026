interface:
  display_name: "公众号HTML组件"
  short_description: "内部组件：生成HTML并创建草稿"
  default_prompt: "Use $wechat-html-publisher only as an internal component of $wechat-article-pipeline to render article.html from .wechat-work/article.json and create a WeChat draft."
policy:
  allow_implicit_invocation: false
